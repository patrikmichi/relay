# Cline provider config — added YAML-ONLY, with NO Go change, to prove the
# config-driven refactor's central claim: a normal new platform is a data
# edit, not a recompile.
#
# Provenance: best-effort per the common Agent-Skills global+project
# convention (see vault/research/repos/2026/05/02/asm-universal-skill-manager.md,
# which lists Cline among ASM's 18 built-in provider configs). Confirm
# against Cline's own docs before relying on this in production.
id: cline
skill_file: SKILL.md
resource_dirs: [scripts, references, assets]
discovery: direct

dirs:
  user:
    - { path: "~/.cline/skills", role: own }
  project:
    - { path: ".cline/skills", role: own }

frontmatter:
  - { ir: name, key: name, type: string, presence: required }
  - { ir: description, key: description, type: string, presence: required }
  - { ir: metadata, key: metadata, type: map, presence: optional }

capabilities: [Metadata]
